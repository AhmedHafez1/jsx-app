import React from 'react';
import ReactDOM from 'react-dom/client';

const rootEl = document.getElementById('root');

const root = ReactDOM.createRoot(rootEl);

function App() {
  return <h1>بسم الله</h1>;
}

root.render(<App />);
